.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
.env
.DS_Store
.vscode/
.idea/
*.log

# Generation workspace scratch files
*_brief.md
*_snippet.py
api_generated.py
*.bak

# Local tooling for the generate-then-verify workflow. Deliberately not part of
# the distributed package: they drive a local Ollama model that only exists on
# the author's machine, so they are noise to anyone else reading the repo.
generate_from_brief.py
generate_tests.py

# Working notes and handoff docs kept alongside the project, not shipped with
# it. CLAUDE.md itself IS tracked, at the repo root where it loads as context.
Claude/

# Separate project — not part of this library
company-agents/
